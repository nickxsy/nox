import { defineConfig } from 'oxfmt';

export default defineConfig({
  printWidth: 80,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: 'all',
  sortImports: true,
  useTabs: false,
  semi: true,
  sortPackageJson: true,
});