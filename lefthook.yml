pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{ts,tsx}'
      run: pnpm lint
    format:
      glob: '*.{ts,tsx}'
      run: pnpm fmt:check

pre-push:
  commands:
    test:
      run: pnpm test