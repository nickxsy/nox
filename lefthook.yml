pre-commit:
  parallel: true
  commands:
    formatting:
      glob: '*.{js,ts,jsx,tsx,md,mdx,json,yml}'
      run: pnpm fmt:check
    lint:
      glob: '*.{js,ts,jsx,tsx}'
      run: pnpm lint

pre-push:
  commands:
    test:
      run: pnpm test